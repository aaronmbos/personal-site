name: Create post image

on:
  workflow_dispatch:
  push:
    branches: ["main", "q2/feat/post-images-gha"]

jobs:
  build:
    # if: contains(github.ref_name, '/post/')
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y imagemagick jq curl zip

      - name: Install IBM Plex Sans font
        run: |
          mkdir /tmp/fonts
          curl -L https://github.com/IBM/plex/releases/download/%40ibm%2Fplex-serif%401.0.0/ibm-plex-serif.zip -o ibm-plex-serif.zip
          unzip ibm-plex-serif.zip -d /tmp/fonts
          cp /tmp/fonts/fonts/complete/ttf/IBMPlexSans-SemiBold.ttf ~/.local/share/fonts/
          fc-cache -f -v

      - name: Execute create image script
        run: |
          chmod +x ./.github/workflows/create-post-image.sh
          ./.github/workflows/create-post-image.sh
